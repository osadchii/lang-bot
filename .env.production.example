# Production Environment Configuration Example
# Greek Language Learning Bot
#
# IMPORTANT: This file is for documentation purposes only.
# DO NOT copy this file directly to production.
# Instead, use secrets.env encrypted with SOPS for sensitive values.
#
# For local development, copy this to .env and fill in your values.

# ============================================================================
# Telegram Bot Configuration
# ============================================================================

# Your Telegram Bot API token from @BotFather
# Example: 123456789:ABCdefGhIjKlmNoPqRsTuVwXyZ1234567890
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# ============================================================================
# OpenAI Configuration
# ============================================================================

# Your OpenAI API key from https://platform.openai.com/api-keys
# Example: sk-proj-abc123...
OPENAI_API_KEY=sk-your_openai_api_key_here

# OpenAI model to use for AI features
# Options: gpt-4, gpt-4-turbo, gpt-3.5-turbo
# Recommended: gpt-4 for best quality
OPENAI_MODEL=gpt-4

# ============================================================================
# Database Configuration
# ============================================================================

# PostgreSQL database user
# Default: postgres
POSTGRES_USER=postgres

# PostgreSQL database password
# IMPORTANT: Use a strong, random password in production
# Generate with: openssl rand -base64 32
POSTGRES_PASSWORD=your_strong_random_password_here

# PostgreSQL database name
# Default: langbot
POSTGRES_DB=langbot

# Database connection URL (constructed automatically by docker-compose)
# Format: postgresql+asyncpg://user:password@host:port/database
# Example: postgresql+asyncpg://postgres:password@db:5432/langbot
# NOTE: This is set automatically by docker-compose, but can be overridden
# DATABASE_URL=postgresql+asyncpg://postgres:your_password@db:5432/langbot

# ============================================================================
# Application Configuration
# ============================================================================

# Enable debug mode (DO NOT use in production)
# Options: True, False
# Default: False
DEBUG=False

# Logging level
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Recommended: INFO for production, DEBUG for development
LOG_LEVEL=INFO

# ============================================================================
# Production-Specific Notes
# ============================================================================
#
# 1. NEVER commit .env files with real secrets to version control
# 2. Use SOPS to encrypt secrets.env before committing
# 3. GitHub Actions will decrypt secrets.env during deployment
# 4. Ensure all required secrets are configured in GitHub Secrets:
#    - SOPS_AGE_KEY (for decryption)
#    - SSH_PRIVATE_KEY (for deployment)
#    - SSH_HOST (server IP or domain)
#    - SSH_USER (SSH username)
# 5. On production server, secrets are managed via encrypted secrets.env
